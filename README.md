# Jenkins CI/CD Pipeline on AWS (Local + EC2)

This project demonstrates a Jenkins-based CI/CD pipeline executed in two environments:
- Local Jenkins (Dockerized)
- Jenkins on AWS EC2

The pipeline covers build, test, Dockerization, failure handling, and clean teardown.

Detailed documentation is available in the `docs/` directory.
